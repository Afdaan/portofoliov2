// Lightweight replacement for Decorations component
// Disabled for performance optimization

const Decorations = () => {
  return null // No decorative animations for better performance
}

export default Decorations
